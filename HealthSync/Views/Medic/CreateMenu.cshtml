@model Menu;

@{
    ViewData["Title"] = "CreateMenu";
}

<h1>Register your menu</h1>

<form asp-action="CreateMenu">
    <partial name="_Form" />
    <button class="btn btn-primary">Register</button>
    <a asp-action="Index" class="btn btn-primary">Back to Home</a>
</form>
